// src/environments/environment.prod.ts
// Production environment configuration
// Used automatically when running `ng build --configuration production`

export const environment = {
  production: true,
  apiBaseUrl: 'https://your-production-api.com/api',   // <-- update before deploying
  appName: 'Simple Healthcare',
  version: '1.0.0',
};
