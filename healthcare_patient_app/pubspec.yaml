name: healthcare_patient_app
description: A new Flutter project for Healthcare Patient Portal.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.3.3
  provider: ^6.0.5
  flutter_secure_storage: ^9.0.0
  jwt_decoder: ^2.0.1
  google_fonts: ^6.1.0
  intl: 0.20.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
